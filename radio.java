import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class radio {
    public static void main(String[] args){
        System.setProperty("webdriver.chrome.driver","D://chromedriver_win32//chromedriver.exe");
        WebDriver driver = new ChromeDriver();

        // String baseUrl="http://test.rubywatir.com/radios.php";
        // driver.get(baseUrl);
        // WebElement radio1= driver.findElement(By.cssSelector("input[value=Radio1]"));
        // System.out.println("radio1 is checked: "+radio1.isSelected());
        // String[] webStrings={"Nope","No"};
        //     driver.findElements(By.cssSelector("input[value=webstrings[i]"));
        //     driver.findElements(By.id());
        // driver.close();
        // for(int i=0;i<2;++i){

        // }
        int g=3;
        System.out.println(++g*8);

    }
    
}
